#include <stdio.h>
#include <string.h>
/**
 * main - Prints all alphabets in lowercase
 * return - Always 0
 */

void print_alphabet(void);
{
	print_alphabet();
	int i;

	for (i = 97; i <= 122; i++)
	{
		putchar(i);
	}
	putchar("/n");
	return (0);
}

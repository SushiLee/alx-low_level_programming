#include <stdio.h>

/**
 * main - Prints all single-digit numbers
 * in base 10
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i = 0;

		for(i = 48; i < 58; i++)
		{
			putchar(i);
		}	
		{
		putchar('\n');
		}
	return (0);
}

